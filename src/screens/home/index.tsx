import React from 'react';
import { Text, View } from "react-native";

export const home = (props: any) => {
    return (
        <View style={{flex: 1}}>
            {/* header */}
            {/* main */}
            {/* footer */}
        </View>
    )
}